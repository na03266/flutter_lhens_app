import 'dart:ui';

import 'package:flutter/cupertino.dart';

class AppPaddings {
  // background
  static const home = EdgeInsets.all(30);
  static const title = EdgeInsets.only(top: 20);
  static const titleCustom = EdgeInsets.only(left: 30, top: 20, right: 30);
  static const homeNoRight = EdgeInsets.only(left: 30, top: 20);
  static const border = EdgeInsets.only(top: 10, bottom: 10);
  static const menu = EdgeInsets.only(left: 20, right: 20);
  static const box = EdgeInsets.only(top: 2, right: 12, bottom: 3, left: 11);
  static const tab = EdgeInsets.only(top: 9, right: 29, bottom: 10, left: 30);


  static const img = EdgeInsets.only(right: 10);

}