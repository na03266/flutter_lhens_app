import 'package:flutter/cupertino.dart';

class AppBorderRadius {
  static const BorderRadius radius9 = BorderRadius.all(Radius.circular(9));
  static const BorderRadius radius10 = BorderRadius.all(Radius.circular(10));
  static const BorderRadius radius11 = BorderRadius.all(Radius.circular(11));
  static const BorderRadius radius12 = BorderRadius.all(Radius.circular(12));
  static const BorderRadius radius13 = BorderRadius.all(Radius.circular(13));
  static const BorderRadius radius15 = BorderRadius.all(Radius.circular(15));
  static const BorderRadius bottomLeftRight37 = BorderRadius.only(bottomLeft: Radius.circular(37), bottomRight: Radius.circular(37));
  static const BorderRadius radius60 = BorderRadius.all(Radius.circular(60));
}